import styled from "styled-components";

const Card = () => {
  return (
    <Wrapper>
      
    </Wrapper>
  )
}

export default Card;

const Wrapper = styled.section`
  width: 180px;
  height: 100px;
  background-color: white;
  border-radius: 5px;
`